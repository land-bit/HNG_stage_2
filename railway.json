{
  "build": {
    "builder": "nixpacks",
    "nixpacks": {
      "phases": {
        "install": {
          "cmd": "pnpm install --no-frozen-lockfile"
        },
        "start": {
          "cmd": "pnpm run start"
        }
      }
    }
  }
}
